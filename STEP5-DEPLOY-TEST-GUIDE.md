# 🐝 STEP 5 — Deploy ও বাস্তব টেস্ট গাইড
এটা আপনার নিজের কম্পিউটার/ফোনে চালানোর জন্য — Claude এখান থেকে কিছুই সরাসরি চালায়নি।

---

## ধাপ ১ — GEMINI_API_KEY Secret যাচাই

টার্মিনালে (আপনার কম্পিউটারে, Firebase CLI ইনস্টল থাকা অবস্থায়):

```bash
firebase functions:secrets:access GEMINI_API_KEY
```

- যদি key-টার একটা মান দেখায় → ✅ ইতিমধ্যে সেট আছে
- যদি "secret not found" এররর দেখায় → নিচেরটা চালান:

```bash
firebase functions:secrets:set GEMINI_API_KEY
```
(এখানে টার্মিনাল আপনার কাছে key-টা পেস্ট করতে চাইবে — টাইপ/পেস্ট করে Enter দিন)

⚠️ **এই key-টা কখনো আমাকে (চ্যাটে) পাঠাবেন না বা কোনো স্ক্রিনশটে দেখাবেন না।**

**চেকলিস্ট:**
- [ ] `firebase functions:secrets:access GEMINI_API_KEY` — সফল হয়েছে (মান দেখিয়েছে, এরর না)

---

## ধাপ ২ — Cloud Function Deploy

```bash
cd আপনার-প্রজেক্ট-ফোল্ডার/functions
npm install    # যদি আগে না করা থাকে
cd ..
firebase deploy --only functions:parseShoppingIntent
```

(শুধু এই একটা ফাংশন — বাকিগুলো (dispatch ইঞ্জিন ইত্যাদি) স্পর্শ করার দরকার নেই, যদি সেগুলো এখনো deploy করার অনুমতি না দিয়ে থাকেন)

Deploy শেষে টার্মিনালে "✔ functions[parseShoppingIntent(...)]" এর মতো একটা সবুজ লাইন দেখাবে।

**চেকলিস্ট:**
- [ ] Deploy কমান্ড এরর ছাড়া শেষ হয়েছে
- [ ] Firebase Console → Functions ট্যাবে `parseShoppingIntent` তালিকায় দেখা যাচ্ছে
- [ ] Console-এ ফাংশনে ক্লিক করে "Configuration" ট্যাবে `GEMINI_API_KEY` secret bound দেখাচ্ছে

---

## ধাপ ৩ — Real Gemini টেক্সট টেস্ট

1. Bazar অ্যাপে Google দিয়ে লগইন করুন
2. Assistant খুলুন, টাইপ করুন: **"২ কেজি চাল, ১ কেজি ডাল আর এক লিটার তেল দাও।"**
3. পাঠানোর পর:
   - Firebase Console → Functions → `parseShoppingIntent` → Logs-এ যান
   - সাম্প্রতিক execution-এ ঢুকে দেখুন — **কোনো secret/key value যেন প্রিন্ট না হয়** (আমার কোডে কোথাও `console.log`-এ key প্রিন্ট করা নেই, কিন্তু নিজে একবার চোখে দেখে নিশ্চিত হওয়া ভালো)
4. Cart-এ গিয়ে দেখুন চাল(২কেজি)/ডাল(১কেজি)/তেল(১লিটার) ঠিকভাবে যোগ হয়েছে কিনা

**Gemini failure সিমুলেট করে fallback টেস্ট:**
- সাময়িকভাবে Firebase Console থেকে `GEMINI_API_KEY` secret-টা একটা ভুল মান দিয়ে বদলে দিন (`firebase functions:secrets:set GEMINI_API_KEY` আবার চালিয়ে ভুল কিছু বসান), আবার deploy করুন, আবার Assistant দিয়ে অর্ডার দিন — Cart তবুও ঠিকভাবে ভরে কিনা দেখুন (fallback rule-based পার্সার কাজ করার কথা)। **টেস্ট শেষে আসল key-টা আবার সঠিকভাবে বসিয়ে দিতে ভুলবেন না।**

**চেকলিস্ট:**
- [ ] তিনটা আইটেমই সঠিক qty-তে Cart-এ গেছে
- [ ] Cloud Function logs-এ কোনো key/secret value দেখা যায়নি
- [ ] ভুল key দিয়ে টেস্ট করলে fallback পার্সার কাজ করেছে
- [ ] টেস্ট শেষে আসল key পুনরায় সঠিকভাবে বসানো হয়েছে

---

## ধাপ ৪ — বাস্তব Android ফোনে Voice টেস্ট

ফোনে Chrome দিয়ে Bazar খুলুন (GitHub Pages লিংক), Assistant খুলুন, মাইক্রোফোন পারমিশন দিন।

| # | বলুন | দেখুন | PASS/FAIL |
|---|---|---|---|
| ১ | "২ কেজি চাল আর ১ কেজি ডাল দাও" | দুটোই Cart-এ সঠিক qty-তে | |
| ২ | "এক লিটার তেল আর এক ডজন ডিম লাগবে" | তেল ১লি, ডিম ১২টা | |
| ৩ | "চালটা আরও ২ কেজি বাড়াও" | চালের qty বেড়েছে | |
| ৪ | "ডালটা বাদ দাও" | ডাল সরেছে, বাকিগুলো অক্ষত | |
| ৫ | "আমার বাজারে এখন কী কী আছে?" | সঠিক সারাংশ শোনায়/দেখায় | |
| ৬ | এমন কিছু বলুন যার একাধিক সাইজ আছে | অপশন দেখায়, নিজে বেছে নেয়নি | |
| ৭ | দোকানে নেই এমন কিছু চান | "খুঁজে পাচ্ছি না" বলে, বানায় না | |
| ৮ | ভুল/অস্পষ্টভাবে বলুন | নিশ্চিত হতে জিজ্ঞেস করে, ভুল কিছু cart-এ দেয় না | |
| ৯ | মাঝপথে অন্য জিনিস চান | আগেরটা ঠিক রেখে নতুনটাও যোগ করে | |
| ১০ | Assistant বন্ধ করে পুরনো নিয়মে (Market→Shop→Cart) কিনুন | সব আগের মতোই কাজ করে | |

**বিশেষভাবে দেখুন:**
| প্রশ্ন | উত্তর |
|---|---|
| Assistant নিজের voice আবার শুনে ফেলছে (echo loop) কিনা? | |
| একই কথা দুইবার প্রসেস হচ্ছে কিনা? | |
| Mic পারমিশন "Deny" করলে অ্যাপ ভেঙে যায় নাকি ঠিক থাকে (টেক্সট ইনপুট কাজ করে)? | |
| বাংলা voice output আদৌ শোনা যাচ্ছে, নাকি নিঃশব্দ/ইংরেজি voice? | |
| Frontend-এর কোনো ফাইলে/Network ট্যাবে (ব্রাউজার DevTools) API key দেখা যাচ্ছে কিনা? | |

---

## চূড়ান্ত রিপোর্ট টেমপ্লেট (আমাকে ফেরত পাঠানোর জন্য)

```
ধাপ ১ (Secret): PASS/FAIL
ধাপ ২ (Deploy): PASS/FAIL
ধাপ ৩ (Real Gemini text test): PASS/FAIL
ধাপ ৩ (Fallback test): PASS/FAIL
ধাপ ৪ Voice Test ১-১০: [প্রতিটার PASS/FAIL]
Echo loop সমস্যা: হ্যাঁ/না
Double-processing সমস্যা: হ্যাঁ/না
Mic-deny handling: ঠিক আছে/ভাঙে
বাংলা voice output: শোনা যায়/যায় না
Key frontend-এ দৃশ্যমান: হ্যাঁ/না (হ্যাঁ হলে STOP করে জানাবেন, এটা জরুরি)
কোনো এরর/স্ক্রিনশট: [থাকলে সংযুক্ত করুন, key/secret বাদ দিয়ে]
```

এই রিপোর্টটা পূরণ করে পাঠালে আমি বিশ্লেষণ করব এবং প্রয়োজনে ঠিক করে দেব।
