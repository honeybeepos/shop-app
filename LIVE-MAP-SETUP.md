# 🗺️ লাইভ ম্যাপ + GPS ট্র্যাকিং — সেটআপ গাইড

## 🆕 আপডেট: প্যাকেজ স্ট্যাটাস + কাস্টমার লাইভ ট্র্যাকিং

এখন নতুন করে যোগ হয়েছে:
- **📦 স্ট্যাটাস স্টেজ:** প্যাকিং হচ্ছে → পিকআপ হয়েছে → পথে আছে → ডেলিভারি সম্পন্ন
  (দোকান ও ডেলিভারি ম্যান — দুজনেই আপডেট করতে পারবে)
- **🔗 কাস্টমার ট্র্যাকিং পেজ** (`honey-bee-track.html`, নতুন ফাইল) — লগইন
  ছাড়াই খোলে, একটা শেয়ারযোগ্য লিংকে (WhatsApp-এ পাঠানো যাবে) কাস্টমার
  রাইডারের লাইভ অবস্থান + স্ট্যাটাস দেখতে পারবে
- **রাইডারও এখন কাস্টমারের অবস্থান দেখতে পারবে** (আগে শুধু একমুখী ছিল, এখন
  দ্বিমুখী) — সাথে "🧭 নেভিগেট করুন" বাটন (Google Maps-এ সরাসরি দিকনির্দেশনা,
  key ছাড়াই)

এই ফিচারের জন্য নিচের Rules-এ `deliveryCalls`-এ একটা `allow update: if true;`
লাইন যোগ হয়েছে (সম্পূর্ণ কোডে অন্তর্ভুক্ত করা আছে) — এবং নতুন ফাইল
`honey-bee-track.html` আপলোড করতে হবে।


কার্ড-ফ্রি সমাধান বসানো হয়েছে:
- **ম্যাপ:** Leaflet.js + OpenStreetMap টাইল (সম্পূর্ণ ফ্রি, কোনো API key লাগে না)
- **রুট/দূরত্ব:** OSRM-এর পাবলিক ডেমো সার্ভার (ফ্রি, key লাগে না) — না পাওয়া গেলে
  সরলরেখার দূরত্ব (Haversine ফর্মুলা) দেখানো হয়

---

## ধাপ ১ — Firestore Rules-এ নতুন একটা ব্লক যোগ করুন

আপনার `shops/{shopId}` ব্লকের ভেতরে (যেমন `deliveryDevices` ব্লকের কাছাকাছি)
এই নতুন অংশটা যোগ করুন:

```
match /live/{docId} {
  allow read, write: if true;
}
```

**পুরো `shops/{shopId}` ব্লকটা এখন এরকম দেখাবে** (আগের সব ঠিক আছে, শুধু নিচের
`live` অংশটা নতুন):

```
match /shops/{shopId} {
  allow create: if isSignedIn() && request.auth.uid == shopId;
  allow get: if true;
  allow list: if true;
  allow update: if isSignedIn() &&
    (isSuperAdmin() ||
     (myShopId() == shopId && myRole() == 'admin') ||
     (resource.data.assignedAgentId == request.auth.uid));
  allow delete: if isSuperAdmin();

  match /members/{memberUid} {
    allow read: if isSignedIn() && (isSuperAdmin() || myShopId() == shopId);
    allow write: if isSignedIn() &&
      (isSuperAdmin() || (myShopId() == shopId && myRole() == 'admin'));
  }

  match /appdata/{docId} {
    allow read: if isSignedIn() && (isSuperAdmin() || myShopId() == shopId);
    allow write: if isSignedIn() && myShopId() == shopId;
  }

  match /deliveryCalls/{callId} {
    allow read: if true;
    allow create: if isSignedIn() && myShopId() == shopId;
    allow update: if true;
  }

  match /deliveryDevices/{token} {
    allow read, write: if true;
  }

  match /publicStock/{itemId} {
    allow read: if true;
    allow write: if isSignedIn() && myShopId() == shopId;
  }

  match /live/{docId} {
    allow read, write: if true;
  }
}
```

⚠️ আগের মতোই — `live` ডকুমেন্টেও কোনো লগইন ছাড়া লেখা যায় (ডেলিভারি ম্যান
অ্যাপে লগইন নেই বলে), তাই এটা `deliveryDevices`-এর মতোই "শপ কোড জানা মানেই
বিশ্বাসযোগ্য" এই নীতিতে খোলা রাখা হয়েছে। এখানে শুধু লাইভ GPS কোঅর্ডিনেট থাকে,
কোনো ব্যক্তিগত/আর্থিক তথ্য না।

Publish করতে ভুলবেন না।

---

## ধাপ ২ — নতুন ফাইলগুলো আপলোড করুন

- `shop-ledger-app.html` (আপডেটেড — নতুন বাটন ও লাইভ ম্যাপ)
- `delivery-man-app.html` (আপডেটেড — GPS শেয়ারিং টগল)

---

## ধাপ ৩ — কীভাবে ব্যবহার করবেন

**দোকানের অ্যাপে (shop-ledger-app.html) — Honey Bee Network স্ক্রিনে:**
1. প্রথমবার **"📍 দোকানের অবস্থান সেট করুন"** চাপুন (একবারই লাগবে) — এটা
   দোকানের GPS লোকেশন সেভ করে রাখবে (🏪 মার্কারের জন্য)
2. **"🔔 ডেলিভারি ম্যান কে ডাকুন"** চাপলে এখন একটা ম্যাপ খুলবে — কাস্টমারের
   ঠিকানায় ট্যাপ করে পিন বসান (🏠), তারপর "✅ এই লোকেশন দিয়ে ডাকুন" চাপুন।
   লোকেশন ছাড়া শুধু ডাকতে চাইলে "লোকেশন ছাড়াই শুধু ডাকুন" চাপুন। ডাকার পর
   একটা ট্র্যাকিং লিংক দেখাবে — সেটা কাস্টমারকে WhatsApp-এ পাঠিয়ে দিন।
3. **"🗺️ লাইভ ম্যাপ দেখুন"** চাপলে একটা ম্যাপে দোকান, রাইডারের লাইভ অবস্থান
   (🛵) ও সর্বশেষ কাস্টমারের ঠিকানা (🏠) একসাথে দেখাবে, সাথে রোড-রুট, দূরত্ব,
   আনুমানিক ডেলিভারি চার্জ, এবং প্যাকেজ স্ট্যাটাস বদলানোর বাটন (📦→🛵→🚴→✅)।

**ডেলিভারি ম্যানের অ্যাপে (delivery-man-app.html):**
1. শপ কোড দিয়ে কানেক্ট করার পর **"📍 লোকেশন শেয়ারিং চালু করুন"** চাপুন,
   লোকেশন পারমিশন দিন
2. এখন প্রতি ~৭ সেকেন্ডে তার লাইভ অবস্থান দোকানের ম্যাপে আপডেট হবে
3. নতুন অর্ডার এলে "📦 বর্তমান অর্ডার" কার্ডে কাস্টমারের ঠিকানা (যদি সেট করা
   থাকে) ম্যাপে দেখাবে, সাথে "🧭 কাস্টমারের কাছে নেভিগেট করুন" বাটন (Google
   Maps খুলে যাবে) এবং স্ট্যাটাস আপডেট করার বাটন (পিকআপ/পথে আছি/সম্পন্ন)
4. যেকোনো সময় "⏹️ লোকেশন শেয়ারিং বন্ধ করুন" চাপলে শেয়ারিং বন্ধ হয়ে যাবে

**কাস্টমারের জন্য (honey-bee-track.html — লগইন লাগে না):**
- দোকান থেকে পাঠানো ট্র্যাকিং লিংকে চাপ দিলেই খুলবে
- রাইডারের লাইভ অবস্থান, নিজের ঠিকানার পিন, এবং প্যাকেজ কোন স্টেজে আছে
  (প্যাকিং/পিকআপ/পথে/সম্পন্ন) — সবকিছু রিয়েলটাইমে দেখতে পারবে

---

## ডেলিভারি চার্জের হিসাব কীভাবে হয়

ডিফল্টভাবে: **বেস চার্জ SAR 5 + প্রতি কিমি SAR 1.5**। এটা এখন কোডে হার্ডকোড
করা আছে (`shop-ledger-app.html`-এর `hbEstimateCharge()` ফাংশনে) — চাইলে এই
দুটো সংখ্যা বদলে দিতে পারি, অথবা এজেন্টের সেট করা `deliveryChargeConfig`
(Honey Bee Agent অ্যাপ থেকে) দিয়ে সংযুক্ত করে দিতে পারি — কোনটা চান বললে
পরের ধাপে করে দেব।

---

## ⚠️ সততার সাথে সীমাবদ্ধতাগুলো

- **APK বানানো সম্ভব না** এই পরিবেশ থেকে — এটা একটা ওয়েব অ্যাপ (PWA)। ফোনে
  ব্রাউজার থেকে "Add to Home Screen" করলে প্রায় নেটিভ অ্যাপের মতোই আইকন/
  ফুলস্ক্রিন অভিজ্ঞতা পাবেন, কিন্তু এটা টেকনিক্যালি .apk ফাইল না।
- **ব্যাকগ্রাউন্ড GPS ট্র্যাকিং সীমিত:** ব্রাউজার ট্যাব/অ্যাপ পুরোপুরি বন্ধ বা
  স্ক্রিন অনেকক্ষণ লক থাকলে মোবাইল ব্রাউজার GPS আপডেট থামিয়ে দিতে পারে —
  এটা ব্রাউজারের সীমাবদ্ধতা, সত্যিকারের নেটিভ অ্যাপের মতো ২৪/৭ ব্যাকগ্রাউন্ড
  ট্র্যাকিং পেতে হলে ভবিষ্যতে একটা নেটিভ (React Native/Flutter) অ্যাপ বানানো
  লাগবে।
- **OSRM পাবলিক ডেমো সার্ভার** ফ্রি কিন্তু ভারী ব্যবহারে (অনেক দোকান/অনেক কল
  একসাথে) রেট-লিমিট হতে পারে — তখন এটা স্বয়ংক্রিয়ভাবে সরলরেখার দূরত্বে
  ফলব্যাক করবে (অ্যাপ ভেঙে পড়বে না, শুধু রুট লাইনটা সরলরেখা দেখাবে)। বড়
  স্কেলে গেলে নিজের OSRM সার্ভার হোস্ট করা (এখনো ফ্রি/ওপেন-সোর্স, শুধু
  নিজের সার্ভার লাগবে) ভালো সমাধান হবে।
